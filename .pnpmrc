# Force the use of specific package versions
save-exact=true
# Prevent automatic installation of optional dependencies
optional=false
# Use strict peer dependencies
strict-peer-dependencies=false
# Specify Tailwind CSS version
tailwindcss=3.3.3
